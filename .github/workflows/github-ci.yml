name: GitHub CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build and test
    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
